int pp1 = 10;
int pp2 = 11;

void setup() {
  pinMode(pp1, OUTPUT);
  pinMode(pp2, OUTPUT);
}

void loop() {

 for (int x = 0; x <= 10; x++){
  analogWrite (pp1, x*10);
  analogWrite (pp2, x*10); 
  delay(2000);
  } 
  analogWrite (pp1, 0);
}
